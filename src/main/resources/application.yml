server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/jwt_db
    username: root
    password: "@Tha_sql23"
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

logging:
  level:
    root: debug

